//
//  Step.swift
//  Deliveryman
//
//  Created by Dmitriy Mitrofansky on 29.09.24.
//

import SpriteKit

/*
 class Step: SKSpriteNode {
 enum Direction {
 case left, right
 }
 
 fileprivate(set) weak var nextStep: Step?
 fileprivate(set) weak var prevStep: Step?
 let row: Int
 
 init(row: Int, nextStep: Step? = nil, prevStep: Step? = nil) {
 self.nextStep = nextStep
 self.prevStep = prevStep
 self.row = row
 }
 
 required init?(coder aDecoder: NSCoder) {
 fatalError("init(coder:) has not been implemented")
 }
 }
 */
